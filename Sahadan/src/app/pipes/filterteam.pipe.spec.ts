import { FilterteamPipe } from './filterteam.pipe';

describe('FilterteamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterteamPipe();
    expect(pipe).toBeTruthy();
  });
});
